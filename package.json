{
  "name": "tower_defence",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:bundle": "npm run build && node scripts/build-download-bundle.mjs",
    "refresh:standalone": "node scripts/build-download-bundle.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.11",
    "vite-plugin-singlefile": "^2.0.2"
  }
}
